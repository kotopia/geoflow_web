[general]
name=GeoFlow Connector
qgisMinimumVersion=3.28
qgisMaximumVersion=4.99
description=GeoFlow project login, persistent local Snapshot reuse, cache lifecycle management, field-level Changeset synchronization, revision Delta updates, and realtime multi-user Delta hints.
version=0.7.6
author=GeoFlow
email=
about=Thin QGIS client for GeoFlow Server. Version 0.7.6 recovers edits saved to a local Snapshot while production synchronization was disabled, sends them through the reviewed Changeset contract when the project is reopened, keeps QGIS-save-triggered automatic synchronization, and makes server-unsyncable Snapshots read-only. It preserves the managed repository plugin id fix, persistent Snapshot reuse, cache lifecycle rules, canonical UUID behavior, QGIS 4 compatibility, signed-ticket WebSocket realtime, chunked/RTree Snapshots, server streaming/cache work, active-project cache pin control, and the session-expiry reconnect guard from 0.7.5. Pinned projects, pending changes, outbox work, and the active package remain protected from automatic cleanup. Production realtime remains disabled pending a separately reviewed Redis-backed channel layer and ASGI deployment.
category=Database
experimental=True
deprecated=False
tracker=
repository=
homepage=https://geoflow.co.kr
tags=geoflow,gis,qgis,qfield,postgis,geopackage,snapshot,cache,lifecycle,pin,changeset,delta,sync,websocket,realtime,session
