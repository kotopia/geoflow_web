[general]
name=GeoFlow Connector
qgisMinimumVersion=3.28
qgisMaximumVersion=4.99
description=GeoFlow project login, local GeoPackage editing, field-level Changeset synchronization, revision Delta updates, and realtime multi-user Delta hints.
version=0.6.3
author=GeoFlow
email=
about=Thin QGIS client for GeoFlow Server. Version 0.6.3 keeps the validated Changeset/Delta, edit-mode handling, canonical UUID behavior, QGIS 4 compatibility, and low-rate fallback polling from 0.6.2. QGIS WebSocket authentication now uses a short-lived server-signed bearer ticket obtained over the Connector's already-authenticated HTTP session, avoiding brittle cross-stack Django session cookie replay between urllib and QtWebSocket. WebSocket remains the preferred low-load path; 15-second Delta polling is recovery-only and incoming Delta is deferred while unsaved local edits exist. Whole-GeoPackage diff sync remains only as a gated fallback. Production realtime remains disabled pending a separately reviewed Redis-backed channel layer and ASGI deployment.
category=Database
experimental=True
deprecated=False
tracker=
repository=
homepage=
tags=geoflow,gis,qgis,qfield,postgis,geopackage,changeset,delta,sync,websocket,realtime
