[general]
name=GeoFlow Connector
qgisMinimumVersion=3.28
qgisMaximumVersion=4.99
description=GeoFlow project login, persistent local Snapshot reuse, cache lifecycle management, field-level Changeset synchronization, revision Delta updates, and realtime multi-user Delta hints.
version=0.7.3
author=GeoFlow
email=
about=Thin QGIS client for GeoFlow Server. Version 0.7.3 keeps the validated persistent Snapshot reuse, safe cache lifecycle rules, Changeset/Delta, canonical UUID behavior, QGIS 4 compatibility, signed-ticket WebSocket realtime, chunked/RTree Snapshots, server streaming/cache work, and active-project cache pin control from 0.7.2. It now detects an expired GeoFlow browser session after a realtime socket disconnect and stops Delta polling/WebSocket reconnect traffic until the user logs in again, preventing stale development sessions from generating repeated 302/login and WSREJECT traffic after a development-server restart. Pinned projects, pending changes, outbox work, and the active package remain protected from automatic cleanup. Whole-GeoPackage diff sync remains only as a gated fallback. Production realtime remains disabled pending a separately reviewed Redis-backed channel layer and ASGI deployment.
category=Database
experimental=True
deprecated=False
tracker=
repository=
homepage=
tags=geoflow,gis,qgis,qfield,postgis,geopackage,snapshot,cache,lifecycle,pin,changeset,delta,sync,websocket,realtime,session
