[general]
name=GeoFlow Connector
qgisMinimumVersion=3.28
qgisMaximumVersion=4.99
description=GeoFlow project login, local GeoPackage editing, field-level Changeset synchronization, and revision Delta updates.
version=0.5.2
author=GeoFlow
email=
about=Thin QGIS client for GeoFlow Server. Authenticates against GeoFlow, lists only authorized GIS projects, receives the server Layer Plan, and materializes a project-scoped local GeoPackage. Version 0.5.2 keeps the Changeset/Delta and edit-mode handling from 0.5.1, canonicalizes locally generated UUID text without braces, and matches Delta targets by canonical UUID so a locally created feature is not duplicated when its own server Delta is replayed. Existing local packages that already contain semantic UUID duplicates should be discarded and reopened from a fresh Snapshot. Whole-GeoPackage diff sync remains only as a gated fallback when Changeset support is not active. Production synchronization remains disabled pending separate review.
category=Database
experimental=True
deprecated=False
tracker=
repository=
homepage=
tags=geoflow,gis,qgis,qfield,postgis,geopackage,changeset,delta,sync
