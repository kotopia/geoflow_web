[general]
name=GeoFlow Connector
qgisMinimumVersion=3.28
qgisMaximumVersion=4.99
description=GeoFlow project login, local GeoPackage editing, field-level Changeset synchronization, revision Delta updates, and realtime multi-user Delta hints.
version=0.6.2
author=GeoFlow
email=
about=Thin QGIS client for GeoFlow Server. Version 0.6.2 keeps the validated Changeset/Delta, edit-mode handling, canonical UUID behavior, and QGIS 4 realtime compatibility from 0.6.1. It filters WebSocket cookies to the active GeoFlow host/path so duplicate stale session cookies are not sent, backs rejected/disconnected WebSocket reconnects off instead of retrying every few seconds, and reduces fallback Delta polling from 1.5 seconds to 15 seconds. WebSocket remains the preferred low-load path; polling is recovery-only. Incoming Delta is deferred while unsaved local edits exist. Whole-GeoPackage diff sync remains only as a gated fallback. Production realtime remains disabled pending a separately reviewed Redis-backed channel layer and ASGI deployment.
category=Database
experimental=True
deprecated=False
tracker=
repository=
homepage=
tags=geoflow,gis,qgis,qfield,postgis,geopackage,changeset,delta,sync,websocket,realtime
