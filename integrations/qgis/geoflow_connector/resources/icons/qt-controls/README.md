These unchanged PNGs are copied from the installed Qt runtime resources:

- `:/qt-project.org/styles/commonstyle/images/arrow-up-16.png`
- `:/qt-project.org/styles/commonstyle/images/arrow-down-16.png`

They are registered by `resources/feather.qrc` under `/geoflow/feather` so
Qt Designer and the plugin use the same project-owned resource paths.
The images originate from Qt's `qtbase` commonstyle assets; Qt's licensing applies.
