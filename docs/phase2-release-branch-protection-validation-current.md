# Phase 2 release branch protection validation

Repository-only marker for the ordinary pull request used to validate the reviewed release protection policy.

Expected required checks are `release-preflight`, `migration-rehearsal`, and `public-https-smoke`. The pull request should remain open until repository protection is configured and verified.
