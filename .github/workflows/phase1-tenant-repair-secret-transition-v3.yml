name: DEPRECATED - Phase 1 tenant repair and secret transition v3

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Block completed one-shot production transition
        shell: bash
        run: |
          echo 'deprecated_workflow=yes'
          echo 'reason=production_transition_completed_successfully'
          exit 1
