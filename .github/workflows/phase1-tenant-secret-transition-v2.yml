name: DEPRECATED - Phase 1 tenant secret transition v2

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Block retired transition workflow
        shell: bash
        run: |
          echo 'deprecated_workflow=yes'
          echo 'reason=historical_tenant_secret_transition_retired'
          exit 1
