name: Phase 1 tenant secret transition

on:
  workflow_dispatch:
    inputs:
      ssh_port:
        description: SSH port for the reviewed GeoFlow host
        required: false
        default: "22"

permissions:
  contents: read

jobs:
  registry-placeholder:
    if: github.ref_name == 'release/stabilized-deploy'
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Use the release/stabilized-deploy workflow definition.'
