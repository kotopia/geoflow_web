name: Phase 1 tenant secret transition

on:
  push:
    branches:
      - release/stabilized-deploy
    paths:
      - .github/workflows/phase1-tenant-secret-transition.yml
  workflow_dispatch:
    inputs:
      ssh_port:
        description: SSH port for the reviewed GeoFlow host
        required: false
        default: "22"

permissions:
  contents: read

jobs:
  tenant-secret-transition:
    if: github.ref_name == 'release/stabilized-deploy'
    runs-on: ubuntu-latest
    timeout-minutes: 40
    environment: production
    env:
      SSH_HOST: ${{ secrets.GEOFLOW_DEPLOY_HOST }}
      SSH_USER: ${{ secrets.GEOFLOW_DEPLOY_USER }}
      SSH_KEY: ${{ secrets.GEOFLOW_DEPLOY_SSH_KEY }}
      SSH_KNOWN_HOSTS: ${{ secrets.GEOFLOW_DEPLOY_KNOWN_HOSTS }}
      SSH_PORT: ${{ inputs.ssh_port || '22' }}

    steps:
      - name: Checkout reviewed release candidate
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Validate connection inputs
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse --abbrev-ref HEAD)" = "release/stabilized-deploy"
          test -n "$SSH_HOST" || { echo 'missing_secret=GEOFLOW_DEPLOY_HOST'; exit 2; }
          test -n "$SSH_USER" || { echo 'missing_secret=GEOFLOW_DEPLOY_USER'; exit 2; }
          test -n "$SSH_KEY" || { echo 'missing_secret=GEOFLOW_DEPLOY_SSH_KEY'; exit 2; }
          test -n "$SSH_KNOWN_HOSTS" || { echo 'missing_secret=GEOFLOW_DEPLOY_KNOWN_HOSTS'; exit 2; }
          printf '%s' "$SSH_PORT" | grep -Eq '^[0-9]{1,5}$' || { echo 'invalid_ssh_port=yes'; exit 2; }

      - name: Configure strict SSH trust
        shell: bash
        run: |
          set -euo pipefail
          umask 077
          mkdir -p "$HOME/.ssh"
          printf '%s\n' "$SSH_KEY" > "$HOME/.ssh/geoflow_deploy_key"
          printf '%s\n' "$SSH_KNOWN_HOSTS" > "$HOME/.ssh/known_hosts"
          chmod 600 "$HOME/.ssh/geoflow_deploy_key" "$HOME/.ssh/known_hosts"
          ssh-keygen -y -f "$HOME/.ssh/geoflow_deploy_key" >/dev/null

      - name: Move tenant DB passwords to Secrets Manager and enable strict references
        shell: bash
        run: |
          set -euo pipefail
          ssh \
            -i "$HOME/.ssh/geoflow_deploy_key" \
            -p "$SSH_PORT" \
            -o BatchMode=yes \
            -o IdentitiesOnly=yes \
            -o StrictHostKeyChecking=yes \
            -o ConnectTimeout=15 \
            "$SSH_USER@$SSH_HOST" 'bash -s' <<'REMOTE'
          set -euo pipefail

          service='geoflow-stabilized.service'

          fail() {
            echo "tenant_secret_transition_blocker=$1"
            exit 2
          }

          command -v git >/dev/null 2>&1 || fail 'git_missing'
          command -v systemctl >/dev/null 2>&1 || fail 'systemctl_missing'
          command -v curl >/dev/null 2>&1 || fail 'curl_missing'
          command -v sudo >/dev/null 2>&1 || fail 'sudo_missing'
          sudo -n true >/dev/null 2>&1 || fail 'passwordless_sudo_required'

          active="$(systemctl show "$service" -p ActiveState --value 2>/dev/null || true)"
          sub="$(systemctl show "$service" -p SubState --value 2>/dev/null || true)"
          [ "$active" = 'active' ] && [ "$sub" = 'running' ] || fail 'stabilized_service_not_running'

          workdir="$(systemctl show "$service" -p WorkingDirectory --value 2>/dev/null || true)"
          [ -n "$workdir" ] && [ -d "$workdir" ] || fail 'service_working_directory_missing'
          repo="$(git -C "$workdir" rev-parse --show-toplevel 2>/dev/null || true)"
          [ -n "$repo" ] && [ -d "$repo/.git" ] || fail 'service_repo_missing'
          [ -f "$repo/.env" ] || fail 'runtime_dotenv_missing'
          [ -f "$repo/control/services/tenant_db_secret_resolver.py" ] || fail 'tenant_secret_resolver_missing'

          dirty_count="$(git -C "$repo" status --porcelain | wc -l | tr -d ' ')"
          [ "$dirty_count" = '0' ] || fail 'production_worktree_dirty'

          python="$repo/.venv/bin/python"
          [ -x "$python" ] || fail 'stabilized_venv_python_missing'

          before_code="$(curl -sS -o /dev/null -w '%{http_code}' --connect-timeout 5 --max-time 10 http://127.0.0.1:8011/ || true)"
          case "$before_code" in 2??|3??) ;; *) fail 'port_8011_not_healthy_before_transition' ;; esac

          # This Python process intentionally never prints DB passwords, secret payloads,
          # usernames, hosts, group IDs, aliases, or secret identifiers.
          "$python" - "$repo" <<'PY'
          import hmac
          import json
          import os
          import sys

          repo = sys.argv[1]
          sys.path.insert(0, repo)
          os.environ.setdefault("DJANGO_SETTINGS_MODULE", "geoflow_project.settings")

          import django
          django.setup()

          import boto3
          import psycopg2
          from botocore.exceptions import ClientError
          from django.db import transaction

          from control.models import GroupDBConfig
          from control.services.tenant_db_secret_resolver import (
              is_tenant_db_secret_reference,
              resolve_tenant_db_password,
          )

          alias = "default"
          rows = list(GroupDBConfig.objects.using(alias).order_by("group_id"))
          if not rows:
              raise SystemExit("tenant_secret_transition_blocker=no_tenant_db_configs")

          legacy = [row for row in rows if not is_tenant_db_secret_reference(row.db_password)]
          existing_refs = [row for row in rows if is_tenant_db_secret_reference(row.db_password)]
          print(f"tenant_secret_total={len(rows)}")
          print(f"tenant_secret_legacy_before={len(legacy)}")
          print(f"tenant_secret_refs_before={len(existing_refs)}")

          region = str(os.environ.get("AWS_REGION") or "ap-northeast-2").strip()
          sm = boto3.client("secretsmanager", region_name=region)

          planned = []
          for row in rows:
              if is_tenant_db_secret_reference(row.db_password):
                  # Existing references must resolve before anything is changed.
                  resolved = resolve_tenant_db_password(row.db_password, client=sm)
                  if not resolved:
                      raise SystemExit("tenant_secret_transition_blocker=existing_reference_unresolvable")
                  planned.append((row, row.db_password, resolved))
                  continue

              raw = str(row.db_password or "")
              if not raw:
                  raise SystemExit("tenant_secret_transition_blocker=legacy_password_empty")

              secret_id = f"geoflow/tenant-db/{row.group_id}/password"
              reference = f"aws-secretsmanager:{secret_id}#password"
              payload = json.dumps({"password": raw}, separators=(",", ":"))

              try:
                  sm.create_secret(Name=secret_id, SecretString=payload)
              except ClientError as exc:
                  code = str(exc.response.get("Error", {}).get("Code") or "")
                  if code not in {"ResourceExistsException", "ResourceExists"}:
                      raise SystemExit("tenant_secret_transition_blocker=secretsmanager_create_failed") from exc

              try:
                  response = sm.get_secret_value(SecretId=secret_id)
                  secret_string = response.get("SecretString")
                  parsed = json.loads(secret_string) if isinstance(secret_string, str) else None
                  roundtrip = parsed.get("password") if isinstance(parsed, dict) else None
              except Exception as exc:
                  raise SystemExit("tenant_secret_transition_blocker=secretsmanager_roundtrip_failed") from exc

              if not isinstance(roundtrip, str) or not hmac.compare_digest(roundtrip, raw):
                  raise SystemExit("tenant_secret_transition_blocker=existing_secret_mismatch")

              planned.append((row, reference, roundtrip))

          # Prove every tenant credential works before replacing plaintext in central DB.
          for row, _reference, password in planned:
              try:
                  conn = psycopg2.connect(
                      dbname=row.db_name,
                      user=row.db_user,
                      password=password,
                      host=row.db_host,
                      port=row.db_port,
                      sslmode="require",
                      connect_timeout=10,
                  )
                  try:
                      with conn.cursor() as cursor:
                          cursor.execute("SELECT 1")
                          value = cursor.fetchone()
                          if not value or value[0] != 1:
                              raise RuntimeError("unexpected SELECT 1 result")
                  finally:
                      conn.close()
              except Exception as exc:
                  raise SystemExit("tenant_secret_transition_blocker=tenant_connection_validation_failed") from exc

          changed = 0
          with transaction.atomic(using=alias):
              for row, reference, _password in planned:
                  if str(row.db_password) == reference:
                      continue
                  updated = (
                      GroupDBConfig.objects.using(alias)
                      .filter(group_id=row.group_id, db_password=row.db_password)
                      .update(db_password=reference)
                  )
                  if updated != 1:
                      raise RuntimeError("concurrent tenant DB config change detected")
                  changed += 1

          after = list(GroupDBConfig.objects.using(alias).order_by("group_id"))
          if len(after) != len(rows):
              raise SystemExit("tenant_secret_transition_blocker=tenant_config_count_changed")
          if any(not is_tenant_db_secret_reference(row.db_password) for row in after):
              raise SystemExit("tenant_secret_transition_blocker=plaintext_reference_remains")
          for row in after:
              resolved = resolve_tenant_db_password(row.db_password, client=sm)
              if not resolved:
                  raise SystemExit("tenant_secret_transition_blocker=post_update_reference_unresolvable")

          identities = {(str(r.db_host).strip().lower(), int(r.db_port), str(r.db_user)) for r in after}
          print(f"tenant_secret_rows_changed={changed}")
          print(f"tenant_secret_refs_after={len(after)}")
          print("tenant_secret_plaintext_after=0")
          print(f"tenant_db_unique_login_identities={len(identities)}")
          print("tenant_secret_reference_transition_ready=yes")
          PY

          "$python" "$repo/manage.py" check_tenant_db_secret_refs --strict

          env_backup="$(mktemp)"
          chmod 600 "$env_backup"
          cp "$repo/.env" "$env_backup"
          cleanup_backup() { rm -f "$env_backup"; }
          trap cleanup_backup EXIT

          "$python" - "$repo/.env" <<'PY'
          from pathlib import Path
          import os
          import sys
          import tempfile

          path = Path(sys.argv[1])
          original = path.read_text(encoding="utf-8")
          lines = original.splitlines()
          key = "TENANT_DB_REQUIRE_SECRET_REFERENCES"
          out = []
          replaced = False
          for line in lines:
              stripped = line.lstrip()
              if stripped.startswith(key + "="):
                  if not replaced:
                      out.append(f"{key}=1")
                      replaced = True
                  continue
              out.append(line)
          if not replaced:
              out.append(f"{key}=1")
          text = "\n".join(out) + "\n"
          fd, tmp = tempfile.mkstemp(prefix=".env.phase1.", dir=str(path.parent))
          try:
              os.fchmod(fd, 0o600)
              with os.fdopen(fd, "w", encoding="utf-8") as handle:
                  handle.write(text)
                  handle.flush()
                  os.fsync(handle.fileno())
              os.replace(tmp, path)
          finally:
              if os.path.exists(tmp):
                  os.unlink(tmp)
          PY

          restart_ok=no
          if sudo -n systemctl restart "$service"; then
            for _ in $(seq 1 30); do
              active="$(systemctl show "$service" -p ActiveState --value 2>/dev/null || true)"
              sub="$(systemctl show "$service" -p SubState --value 2>/dev/null || true)"
              code="$(curl -sS -o /dev/null -w '%{http_code}' --connect-timeout 2 --max-time 5 http://127.0.0.1:8011/ || true)"
              if [ "$active" = 'active' ] && [ "$sub" = 'running' ]; then
                case "$code" in 2??|3??) restart_ok=yes; break ;; esac
              fi
              sleep 2
            done
          fi

          if [ "$restart_ok" != yes ]; then
            cp "$env_backup" "$repo/.env"
            sudo -n systemctl restart "$service" || true
            fail 'service_failed_with_strict_secret_references'
          fi

          "$python" "$repo/manage.py" check_tenant_db_secret_refs --strict
          "$python" "$repo/manage.py" check_release_preflight --strict

          terms_code="$(curl --proto '=https' -sS -o /dev/null -w '%{http_code}' --connect-timeout 5 --max-time 15 https://geoflow.co.kr/terms/ || true)"
          privacy_code="$(curl --proto '=https' -sS -o /dev/null -w '%{http_code}' --connect-timeout 5 --max-time 15 https://geoflow.co.kr/privacy/ || true)"
          [ "$terms_code" = '200' ] || fail 'public_terms_not_200_after_secret_transition'
          [ "$privacy_code" = '200' ] || fail 'public_privacy_not_200_after_secret_transition'

          echo 'tenant_db_secret_references_enforced=yes'
          echo 'phase1_tenant_secret_transition_ready=yes'
          REMOTE
