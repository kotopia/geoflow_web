name: iroomsng upstream readiness diagnostic launcher

on:
  push:
    branches:
      - release/stabilized-deploy
    paths:
      - .github/workflows/iroomsng-upstream-readiness-launcher.yml

permissions:
  contents: read

jobs:
  launch-reviewed-current-release:
    uses: ./.github/workflows/iroomsng-upstream-readiness-diagnostic.yml
    with:
      ssh_port: "22"
    secrets: inherit
