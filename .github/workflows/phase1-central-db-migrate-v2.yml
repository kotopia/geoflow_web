name: Phase 1 central DB migrate v2

on:
  workflow_dispatch:
    inputs:
      ssh_port:
        description: SSH port for the reviewed GeoFlow host
        required: false
        default: "22"

permissions:
  contents: read

jobs:
  registration-only:
    if: github.ref_name == 'release/stabilized-deploy'
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Run this workflow from release/stabilized-deploy.'
